# Calculate the total expenses
def total_expenses(file_name: str) -> int:
    """
    Calculate the total expenses
    Args:
        file_name: str
    Returns:
        total_expenses: total expenses
    """
    return 